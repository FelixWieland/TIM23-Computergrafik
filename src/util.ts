/**
 * Gets the reference distance used throughout the scene for scaling.
 * This value represents the base unit for positioning and sizing objects in the 3D world.
 * @return The reference distance value in world units
 */
export function getReferenceDistance(): number {
    return 1321;
}
